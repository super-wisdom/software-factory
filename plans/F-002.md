# Plan: Eval runner

ID: F-002   From: specs/F-002.md   Accepted by: founder   Date: 2026-08-31

## Files that change
- src/software_factory/evals.py (new) -- Task/Result, load_tasks, run_task, run_all.
- src/software_factory/cli.py -- add `eval` subcommand + cmd_eval; import run_all.
- tests/test_evals.py (new) -- one test per acceptance criterion.
- evals/tasks/factory-invariants.json (new) -- the five starter tasks.
- .github/workflows/agent-evals.yml -- replace placeholder with `factory eval`.
- evals/README.md -- document how to run and add tasks.

## Order of work
1. evals module: pure load/run/aggregate (argv via shlex, no shell).
2. Wire the eval subcommand into cli.py.
3. Starter tasks JSON.
4. Tests for load/run/rate/exit-codes.
5. Workflow + README.

## Risks
- Quoting in task cmd strings -> use single quotes inside cmd, argv via shlex; covered by
  running the real suite (criterion 1).
- Non-determinism -> tasks restricted to deterministic file/JSON checks.

## Options not taken
- YAML task files (needs a dependency; JSON is stdlib).
- Live-agent evals now (deferred; heavier, needs API budget).

## Proof
- `make check` green.
- `factory eval` reports 5/5 on the starter tasks (demonstrated).
