# Plan: Project init command

ID: F-004   From: specs/F-004.md   Accepted by: founder   Date: 2026-08-31

## Files that change
- src/software_factory/scaffold.py (new) -- generated: FILES = {path: content} for the line,
  base64-encoded so it ships with the package.
- src/software_factory/cli.py -- import scaffold; add package_name(), cmd_init(); wire the
  `init` subcommand.
- tests/test_init.py (new) -- package_name cases, scaffolding + substitution, refuse-nonempty,
  and an end-to-end check that the generated project's pytest passes.
- DELIVERY-TRACKER.md / ADOPTION-TRACKER.md -- bring current (see note below).

## Order of work
1. Assemble the scaffold staging tree (reuse generic line files; tokenise the rest).
2. Generate scaffold.py from it.
3. package_name() + cmd_init() (rename __pkg__, substitute tokens, refuse non-empty).
4. Wire the subcommand.
5. Tests, incl. the generated-project end-to-end check.

## Risks
- Scaffold not shipping after pipx install -> embedded as a Python module, not loose files.
- Token/dir-rename bugs -> covered by substitution + generated-project tests.

## Options not taken
- git clone of a template repo (needs network + a second repo; bundling is offline & self-contained).
- setuptools package-data for loose scaffold files (fragile across pipx; a module is reliable).

## Proof
- `make check` green (this repo).
- `factory init demo-app` produces a project whose own `make check` and `factory eval` pass
  (demonstrated).

## Note
This unit also brings the trackers fully current (F-001..F-003 Shipped, F-004 in flight; AI
review + branch protection marked done), superseding the separate close-F-003 change.
