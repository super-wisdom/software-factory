# Plan: factory new — scaffold a work unit

ID: F-001   From: specs/F-001.md   Accepted by: founder   Date: 2026-08-26

## Files that change
- src/software_factory/cli.py (new) — find_repo_root, next_id, slugify, fill, cmd_new,
  build_parser, main.
- tests/test_cli.py (new) — one test per acceptance criterion.
- pyproject.toml — add [project.scripts] factory = "software_factory.cli:main".
- .claude/skills/artifact-chain/SKILL.md (new) — encodes the workflow that uses the command.

## Order of work
1. Pure helpers first (next_id, slugify, fill) — no I/O, easy to unit-test.
2. find_repo_root — walk up for the marker dirs.
3. cmd_new — compute id, pre-check all three targets, then write; print paths + tracker row.
4. build_parser / main wiring.
5. pyproject console-script entry.
6. Tests for every acceptance criterion.

## Risks
- Placeholder mismatch between fill() and templates/ → covered by criterion 1's test.
- Partial write if the 2nd of 3 targets collides → avoided by pre-checking all three first.

## Options not taken
- Editing the tracker file in place (markdown table parsing is fragile; deferred).
- Exposing this only as a Make target (a real console script is more ergonomic).

## Proof
- `make check` green.
- `factory new "Export to CSV"` in a clean repo produces the three files (demonstrated).
