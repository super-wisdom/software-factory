# Plan: Tracker auto-update

ID: F-003   From: specs/F-003.md   Accepted by: founder   Date: 2026-08-31

## Files that change
- src/software_factory/cli.py -- add inflight_row(), _is_separator(), append_inflight_row();
  call it from cmd_new; add the --no-tracker flag.
- tests/test_cli.py -- add append / idempotency / --no-tracker / missing-tracker tests.
- DELIVERY-TRACKER.md -- bring current: F-001 and F-002 to Shipped, F-003 to In flight.

## Order of work
1. Pure helpers (inflight_row, _is_separator) -- no I/O.
2. append_inflight_row: find "## In flight", its separator, the section body; drop the
   placeholder; skip if id present; insert row; write.
3. Wire into cmd_new behind --no-tracker; adjust the printed message.
4. Tests for every acceptance criterion.

## Risks
- Malformed insert -> constrained to inserting one well-formed row after the separator;
  tested against a realistic table stub.
- Duplicate rows on re-run -> idempotency check by id.

## Options not taken
- A full markdown-table parser/library (overkill; stdlib string handling suffices).
- Auto-moving rows to Shipped (separate future unit).

## Proof
- `make check` green (15 tests).
- `factory new` demonstrated adding a row and clearing the placeholder.
